/* Solarized Dark -- interface style for Xpeccy+
 *
 * Setup -> Debugger -> Style Sheet. "System" there means no style sheet at all.
 *
 * Solarized by Ethan Schoonover (MIT). Low contrast on purpose: the
 * greys sit close together and only the accents carry.
 *
 * Palette:
 *   window #002b36   panel #073642   base #073642   text #93a1a1
 *   selection #268bd2   accent #268bd2   border #144b57
 *
 * No font is set here on purpose: the debugger measures its columns with the
 * font from Setup and this file must not get in the way.
 */

/* ---- base ---------------------------------------------------------- */

QWidget {
	background-color: #002b36;
	color: #93a1a1;
	selection-background-color: #268bd2;
	selection-color: #fdf6e3;
}

QWidget:disabled {
	color: #586e75;
}

/* the emulator screen draws itself, keep the style sheet off it */
MainWin {
	background: transparent;
}

QLabel, QCheckBox, QRadioButton, QGroupBox, QToolBar, QDockWidget {
	background: transparent;
}

QToolTip {
	background-color: #073642;
	color: #93a1a1;
	border: 1px solid #144b57;
	padding: 2px;
}

QFrame[frameShape="4"], QFrame[frameShape="5"] {	/* HLine / VLine */
	color: #144b57;
}

/* ---- text entry ---------------------------------------------------- */

QLineEdit, QPlainTextEdit, QTextEdit, QAbstractSpinBox {
	background-color: #073642;
	color: #93a1a1;
	border: 1px solid #144b57;
	border-radius: 2px;
	padding: 1px 2px;
}

QLineEdit:hover, QAbstractSpinBox:hover {
	border-color: #586e75;
}

QLineEdit:focus, QPlainTextEdit:focus, QTextEdit:focus, QAbstractSpinBox:focus {
	border-color: #268bd2;
}

QLineEdit:disabled, QPlainTextEdit:disabled, QTextEdit:disabled, QAbstractSpinBox:disabled {
	background-color: #002b36;
	color: #586e75;
}

QLineEdit:read-only {
	background-color: #073642;
}

/* ---- buttons ------------------------------------------------------- */

/* min-width and min-height keep the dialog buttons the size the platform
 * style gives them, so a style sheet does not reflow the button rows */

QPushButton {
	background-color: #073642;
	color: #93a1a1;
	border: 1px solid #144b57;
	border-radius: 4px;
	padding: 3px 9px;
	min-width: 53px;
	min-height: 14px;
}

QPushButton:hover {
	background-color: #0b3d49;
	border-color: #586e75;
}

QPushButton:pressed, QPushButton:checked {
	background-color: #002b36;
	border-color: #586e75;
}

QPushButton:default {
	border-color: #268bd2;
}

QPushButton:disabled {
	background-color: #073642;
	color: #586e75;
	border-color: #144b57;
}

QToolButton {
	background: transparent;
	color: #93a1a1;
	border: 1px solid transparent;
	border-radius: 3px;
	padding: 2px;
}

QToolButton:hover {
	background-color: #0b3d49;
	border-color: #144b57;
}

QToolButton:pressed, QToolButton:checked {
	background-color: #002b36;
	border-color: #586e75;
}

QToolButton::menu-indicator {
	image: none;
}

/* ---- combo box ----------------------------------------------------- */

/* The arrows come from the application resources. A style sheet cannot draw
 * one: the CSS border triangle ends up as a filled rectangle in Qt, and
 * leaving the subcontrol alone means the platform draws a bevelled 90s box on
 * a light background. Resource paths are absolute, unlike a relative url()
 * which would be resolved against the working directory.
 * See packaging/make-style-icons.py. */

QComboBox {
	background-color: #073642;
	color: #93a1a1;
	border: 1px solid #144b57;
	border-radius: 2px;
	padding: 1px 2px;
}

QComboBox:hover {
	border-color: #586e75;
}

QComboBox:focus, QComboBox:on {
	border-color: #268bd2;
}

QComboBox:disabled {
	background-color: #002b36;
	color: #586e75;
}

QComboBox::drop-down {
	subcontrol-origin: padding;
	subcontrol-position: center right;
	width: 17px;
	border: none;
	background: transparent;
}

QComboBox::down-arrow {
	image: url(:/images/styles/arrow-down-light.png);
	width: 16px;
	height: 16px;
}

QAbstractSpinBox::up-button, QAbstractSpinBox::down-button {
	subcontrol-origin: border;
	width: 15px;
	border: none;
	background: transparent;
}

QAbstractSpinBox::up-button {
	subcontrol-position: top right;
}

QAbstractSpinBox::down-button {
	subcontrol-position: bottom right;
}

QAbstractSpinBox::up-arrow {
	image: url(:/images/styles/arrow-up-light.png);
	width: 12px;
	height: 12px;
}

QAbstractSpinBox::down-arrow {
	image: url(:/images/styles/arrow-down-light.png);
	width: 12px;
	height: 12px;
}

QComboBox QAbstractItemView {
	background-color: #073642;
	color: #93a1a1;
	border: 1px solid #144b57;
	selection-background-color: #268bd2;
	selection-color: #fdf6e3;
	outline: none;
}

/* ---- check / radio ------------------------------------------------- */

QCheckBox::indicator, QGroupBox::indicator, QMenu::indicator {
	width: 12px;
	height: 12px;
	border: 1px solid #586e75;
	border-radius: 2px;
	background-color: #073642;
}

QRadioButton::indicator {
	width: 12px;
	height: 12px;
	border: 1px solid #586e75;
	border-radius: 7px;
	background-color: #073642;
}

QCheckBox::indicator:hover, QRadioButton::indicator:hover, QGroupBox::indicator:hover {
	border-color: #268bd2;
}

QCheckBox::indicator:checked, QGroupBox::indicator:checked, QMenu::indicator:checked {
	background-color: #268bd2;
	border-color: #268bd2;
	image: url(:/images/styles/check-light.png);
}

QRadioButton::indicator:checked {
	background-color: #268bd2;
	border: 3px solid #073642;
}

QCheckBox::indicator:indeterminate {
	background-color: #586e75;
}

QCheckBox::indicator:disabled, QRadioButton::indicator:disabled {
	background-color: #073642;
	border-color: #144b57;
}

/* ---- group box ----------------------------------------------------- */

/* A style sheet frame brings its own box model: the top margin that makes
 * room for the title pushes every row inside down, which on a group box
 * without a title is a few pixels of drift for nothing. Hence the second
 * rule - untitled boxes keep the geometry they have without a style sheet. */

QGroupBox {
	border: 1px solid #144b57;
	border-radius: 4px;
	margin-top: 8px;
	padding-top: 6px;
}

QGroupBox[title=""] {
	margin-top: 0px;
	padding-top: 0px;
}

QGroupBox::title {
	subcontrol-origin: margin;
	subcontrol-position: top left;
	left: 8px;
	padding: 0px 4px;
	color: #93a1a1;
	background-color: #002b36;
}

/* ---- tabs ---------------------------------------------------------- */

QTabWidget::pane {
	border: 1px solid #144b57;
	top: -1px;
}

QTabBar::tab {
	background-color: #002b36;
	color: #93a1a1;
	border: 1px solid #144b57;
	padding: 3px 6px;
}

QTabBar::tab:top {
	border-bottom: none;
}

QTabBar::tab:bottom {
	border-top: none;
}

QTabBar::tab:!selected:hover {
	background-color: #0b3d49;
	color: #93a1a1;
}

QTabBar::tab:selected {
	background-color: #073642;
	color: #93a1a1;
}

QTabBar::tab:disabled {
	color: #586e75;
}

/* ---- item views ---------------------------------------------------- */

QAbstractItemView {
	background-color: #073642;
	alternate-background-color: #0b3d49;
	color: #93a1a1;
	border: 1px solid #144b57;
	selection-background-color: #268bd2;
	selection-color: #fdf6e3;
	outline: none;
}

QTableView {
	gridline-color: #0e4351;
}

/* :!selected - a hover on the selected row would otherwise put the pale hover
 * colour under the selected text */

QTableView::item:hover:!selected, QTreeView::item:hover:!selected, QListView::item:hover:!selected {
	background-color: #0b3d49;
}

QTreeView::branch {
	background: transparent;
}

QHeaderView {
	background-color: #073642;
	border: none;
}

QHeaderView::section {
	background-color: #073642;
	color: #93a1a1;
	border: none;
	border-right: 1px solid #0e4351;
	border-bottom: 1px solid #144b57;
	padding: 2px 4px;
}

QHeaderView::section:hover {
	background-color: #0b3d49;
}

QTableCornerButton::section {
	background-color: #073642;
	border: none;
	border-right: 1px solid #144b57;
	border-bottom: 1px solid #144b57;
}

/* ---- scroll bars --------------------------------------------------- */

QScrollBar:vertical {
	background-color: #002b36;
	width: 13px;
	margin: 0px;
	border: none;
}

QScrollBar:horizontal {
	background-color: #002b36;
	height: 13px;
	margin: 0px;
	border: none;
}

QScrollBar::handle:vertical {
	background-color: #144b57;
	border-radius: 3px;
	min-height: 20px;
	margin: 1px;
}

QScrollBar::handle:horizontal {
	background-color: #144b57;
	border-radius: 3px;
	min-width: 20px;
	margin: 1px;
}

QScrollBar::handle:hover {
	background-color: #586e75;
}

QScrollBar::add-line, QScrollBar::sub-line {
	background: none;
	border: none;
	width: 0px;
	height: 0px;
}

QScrollBar::add-page, QScrollBar::sub-page {
	background: none;
}

/* ---- sliders / progress -------------------------------------------- */

QSlider::groove:horizontal {
	background-color: #073642;
	border: 1px solid #144b57;
	border-radius: 2px;
	height: 4px;
}

QSlider::groove:vertical {
	background-color: #073642;
	border: 1px solid #144b57;
	border-radius: 2px;
	width: 4px;
}

QSlider::sub-page:horizontal, QSlider::add-page:vertical {
	background-color: #268bd2;
	border-radius: 2px;
}

QSlider::handle:horizontal {
	background-color: #073642;
	border: 1px solid #586e75;
	border-radius: 3px;
	width: 9px;
	margin: -5px 0px;
}

QSlider::handle:vertical {
	background-color: #073642;
	border: 1px solid #586e75;
	border-radius: 3px;
	height: 9px;
	margin: 0px -5px;
}

QSlider::handle:hover {
	background-color: #0b3d49;
	border-color: #268bd2;
}

QProgressBar {
	background-color: #073642;
	color: #93a1a1;
	border: 1px solid #144b57;
	border-radius: 2px;
	text-align: center;
}

QProgressBar::chunk {
	background-color: #268bd2;
}

/* ---- menus --------------------------------------------------------- */

QMenuBar {
	background-color: #073642;
	color: #93a1a1;
	border-bottom: 1px solid #144b57;
}

QMenuBar::item {
	background: transparent;
	padding: 3px 8px;
}

QMenuBar::item:selected {
	background-color: #0b3d49;
}

QMenuBar::item:pressed {
	background-color: #268bd2;
	color: #fdf6e3;
}

QMenu {
	background-color: #073642;
	color: #93a1a1;
	border: 1px solid #144b57;
	padding: 2px;
}

QMenu::item {
	padding: 3px 22px 3px 22px;
	border: 1px solid transparent;
}

QMenu::item:selected {
	background-color: #268bd2;
	color: #fdf6e3;
}

QMenu::item:disabled {
	color: #586e75;
}

QMenu::separator {
	height: 1px;
	background-color: #144b57;
	margin: 3px 6px;
}

QMenu::indicator {
	margin-left: 4px;
}

/* ---- tool bars, docks, status bar ---------------------------------- */

QToolBar {
	border: none;
	spacing: 2px;
	padding: 1px;
}

QToolBar::separator {
	background-color: #144b57;
	width: 1px;
	height: 1px;
	margin: 3px;
}

QStatusBar {
	background-color: #073642;
	color: #93a1a1;
}

QStatusBar::item {
	border: none;
}

QDockWidget {
	color: #93a1a1;
	titlebar-close-icon: none;
	titlebar-normal-icon: none;
}

QDockWidget::title {
	background-color: #073642;
	padding: 3px;
}

QSplitter::handle {
	background-color: #002b36;
}

QSplitter::handle:hover {
	background-color: #268bd2;
}

QSizeGrip {
	background: transparent;
	image: none;
}
